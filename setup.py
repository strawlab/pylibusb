from setuptools import setup

setup(name='pylibusb',
      description='libusb wrapper (ctypes based)',
      version='0.1',
      author='Andrew Straw',
      author_email='strawman@astraw.com',
      packages = ['pylibusb'],
      zip_safe=True)
